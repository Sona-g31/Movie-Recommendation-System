<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login - MovieReco</title>

    <!-- Google Fonts -->
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap"
      rel="stylesheet"
    />

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <style>
      * {
        font-family: "Poppins", sans-serif;
      }

      body {
        background: linear-gradient(to right, #1c92d2, #f2fcfe);
        height: 100vh;
        margin: 0;
        display: flex;
        flex-direction: column;
      }

      .navbar {
        z-index: 10;
      }

      .login-container {
        flex: 1;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
      }

      .login-box {
        background: white;
        padding: 30px;
        border-radius: 16px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
        width: 100%;
        max-width: 400px;
      }

      .form-title {
        font-weight: 600;
        color: #1c92d2;
        margin-bottom: 20px;
        text-align: center;
      }

      .form-control:focus {
        box-shadow: 0 0 0 0.2rem rgba(28, 146, 210, 0.25);
      }

      .btn-primary {
        background-color: #1c92d2;
        border: none;
      }

      .btn-primary:hover {
        background-color: #137dbf;
      }

      .text-center a {
        text-decoration: none;
        color: #137dbf;
      }

      .text-center a:hover {
        text-decoration: underline;
      }
    </style>
  </head>

  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-4">
      <a class="navbar-brand text-white" href="#">Movie Recommedation</a>
      <div class="ms-auto">
        <a href="/home" class="btn btn-outline-light me-2">Home</a>
        <a href="/login" class="btn btn-outline-light me-2">Login</a>
        <a href="/reg" class="btn btn-outline-light">Register</a>
      </div>
    </nav>

    <!-- Login Form -->
    <div class="login-container">
      <div class="login-box">
        <h2 class="form-title">Login</h2>
        <form action="/api/login" method="POST">
          <div class="mb-3">
            <label for="email" class="form-label">Email address</label>
            <input
              type="email"
              name="email"
              class="form-control"
              id="email"
              placeholder="Enter email"
              required
            />
          </div>
          <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <input
              type="password"
              name="password"
              class="form-control"
              id="password"
              placeholder="Enter password"
              required
            />
          </div>
          <button type="submit" class="btn btn-primary w-100 mt-3">
            Login
          </button>
        </form>
        <div class="text-center mt-3">
          <p>Don't have an account? <a href="/reg">Register here</a></p>
        </div>
      </div>
    </div>
    <h4><%=mgs%></h4>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
